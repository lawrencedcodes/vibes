/** @type {import('next').NextConfig} */
const nextConfig = {};

if (process.env.NODE_ENV === 'development') {
    // next-on-pages setup for dev if needed, currently mostly handled by wrangler pages dev
}

module.exports = nextConfig;
